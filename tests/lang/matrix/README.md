# USR Matrix Baselines

This directory contains machine-readable USR registry and matrix artifacts referenced by:

- `docs/specs/unified-syntax-representation.md` section 23
- `docs/specs/usr-registry-schema-contract.md`
- `TES_LAYN_ROADMAP.md` Phase 1

Baseline files are generated by:

- `tools/usr/generate-usr-matrix-baselines.mjs`

Regeneration command:

```powershell
node 'tools/usr/generate-usr-matrix-baselines.mjs'
```
