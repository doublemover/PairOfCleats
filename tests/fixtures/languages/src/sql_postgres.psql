-- Postgres sample
CREATE TABLE pg_widgets (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL
);

CREATE FUNCTION pg_widget_count()
RETURNS INTEGER
LANGUAGE SQL
AS $$
  SELECT COUNT(*) FROM pg_widgets;
$$;
