{
  // Tags excluded unless explicitly included via --tag.
  "excludeTags": [
    "destructive"
  ],
  "lanes": {
    "ci": {
      // CI skips long-running or noisy tests by default.
      "excludeTags": [
        "artifact-size-guardrails",
        "churn-filter",
        "comment-join",
        "context-pack",
        "extracted-prose",
        "fixture-eval",
        "fixture-parity",
        "file-selector-case",
        "hnsw-ann",
        "incremental-cache-signature",
        "incremental-tokenization-cache",
        "lancedb-ann",
        "parity",
        "python-metadata",
        "query-cache",
        "type-signature-decorator",
        "records-index-and-search",
        "search-explain",
        "search-rrf",
        "search-tie-order",
        "shard-merge",
        "summary-report",
        "timeout-target",
        "tool-build-index-progress"
      ]
    }
  }
}
