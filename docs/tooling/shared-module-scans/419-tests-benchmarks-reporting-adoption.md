# Shared-Module Scan: #419

- Issue: `#419`
- Title: `Scan tests, fixtures, benchmarks, and reporting surfaces for missed shared-module adoption`
- Scan date: `2026-03-26`

## Summary

This surface already has good local reuse, especially in:

- [tests/helpers/test-env.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\test-env.js)
- [tests/helpers/test-cache.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\test-cache.js)
- [tests/helpers/api-server.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\api-server.js)
- [tests/helpers/analysis-surface-parity.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\analysis-surface-parity.js)

The right cleanup here is not to hoist test semantics into production shared code. The real adoption work is:

- use the existing test helpers more consistently
- reduce repeated report-generator CLI/JSON boilerplate
- continue consolidating parity-harness setup where it is already clearly shared

2026-05-21 follow-through status: bench-language logging now consumes `createDisplayLoggerAdapter()` from `tools/shared/cli-display.js` for display meta/status routing, while file durability, log rotation, emergency sync closeout, and disk-full history remain bench-local. ONNX env override tests now use `withTemporaryEnv()`, and dict-utils build-root tests now use `prepareTestCacheDir()` plus test-env helpers instead of hand-restored process env state. Generated docs/report writers now share `tools/shared/generated-report.js` for `generatedAt`-preserving JSON output, generated-output comparison normalization, and write-if-changed text output across repo inventory, shared-module ledger, script inventory, doc contract drift, artifact schema index export, generated-surface freshness comparison, runner suite taxonomy/lane evidence governance reports, coverage policy markdown output, config inventory/contract output, release-surface output, demo-config output, triage record markdown, CI JUnit output, benchmark markdown/text output, release notes/readiness/checksum markdown/text output, and their idempotence tests. Report JSON file/stdout emission now uses existing JSON helpers in the matching `combined-summary`, `compare-models`, `diagnostics-report`, `show-throughput`, `report-code-map`, `metrics-dashboard`, config inventory, coverage artifact/policy, triage artifact/context-pack, CI diagnostics, benchmark report, and release metadata/readiness/check/trust/bundle contracts.

2026-05-21 API test-harness follow-through: API SSE parser and streaming request setup now belong to `tests/helpers/api-server.js`. `tests/services/api/server-stream.test.js` consumes `prepareFixtureApiServerCohort()` plus the returned `requestSse()` helper instead of local cache/index/auth/SSE/abort scaffolding, and `tests/services/api/status-repo-error-classification.test.js` consumes the same SSE parser for its in-memory router checks. Evidence is logged in `temp/validation/api-server-stream-helper-20260521.log` and `temp/validation/api-server-stream-helper-fast-final-20260521.log`; `services/api/fixture-api-server-cohort` and `services/api/server-stream` exceeded the 30-second rule and were not rerun.

## Adoption Matrix

### 1. Temp-root and env setup in tests

Shared modules to prefer:
- [test-cache.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\test-cache.js)
- [test-env.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\test-env.js)

Representative local implementations:
- [canonical-workflows.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\cli\general\canonical-workflows.test.js)
- [capability-gate.smoke.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\ci\capability-gate.smoke.test.js)
- [bench-language-rollout-gate.smoke.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\ci\bench-language-rollout-gate.smoke.test.js)
- [onnx-cpu-tuning-and-token-cache.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\indexing\embeddings\onnx-cpu-tuning-and-token-cache.test.js)
- [paths-builds-root.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\tooling\dict-utils\paths-builds-root.test.js)

Best action:
- Prefer the existing test helpers over raw `mkdtemp` or hand-rolled `process.env` mutation when the test is not explicitly asserting raw environment behavior.
- Completed for `onnx-cpu-tuning-and-token-cache.test.js` and `paths-builds-root.test.js`; focused validation is logged in `temp/validation/workspace-allowlist-targeted-validation-rerun-20260521.log`.

### 2. Report generator CLI scaffolding

Shared modules to prefer:
- [cli.js](C:\Users\sneak\Development\DOUBLECLEAT\src\shared\cli.js)
- [stable-json.js](C:\Users\sneak\Development\DOUBLECLEAT\src\shared\stable-json.js)
- [json-writers.js](C:\Users\sneak\Development\DOUBLECLEAT\src\shared\json-stream\json-writers.js)

Representative local implementations:
- [show-throughput.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\show-throughput.js)
- [diagnostics-report.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\diagnostics-report.js)
- [parity-matrix.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\parity-matrix.js)
- [combined-summary.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\combined-summary.js)
- [metrics-dashboard.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\metrics-dashboard.js)
- [report-code-map.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\report-code-map.js)
- [repo-inventory.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\docs\repo-inventory.js)
- [script-inventory.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\docs\script-inventory.js)
- [report.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\test_times\report.js)

Best action:
- Share CLI parsing and JSON/report-writing boilerplate where semantics are identical.
- Keep aggregation and rendering logic local to each report.
- Completed for generated docs/report idempotence output: `tools/docs/repo-inventory.js`, `tools/docs/shared-module-ledger.js`, `tools/docs/script-inventory.js`, `tools/docs/contract-drift.js`, `tools/docs/export-artifact-schema-index.js`, `tools/docs/generated-surfaces.js`, `tests/tooling/docs/repo-inventory-idempotence.test.js`, and `tests/tooling/docs/shared-module-ledger-idempotence.test.js` now use `tools/shared/generated-report.js` where their contracts match. Focused validation is logged in `temp/validation/generated-report-stable-json-final-20260521.log`; the follow-up contract-drift/artifact-schema proof is logged in `temp/validation/generated-report-contract-artifact-followup-clean-20260521.log`; generated-surface comparison/stdout validation is logged in `temp/validation/generated-surfaces-normalization-helper-20260521.log`.
- Completed for report JSON emission/file output where contracts match: `tools/reports/compare-models.js` now uses `writeJsonFileResolved()` for `--out` and keeps stdout on `emitJson()`. Focused validation is logged in `temp/validation/compare-models-json-file-helper-20260521.log`.
- Completed for runner governance generated reports: `tests/runner/suite-taxonomy-report.js` and `tests/runner/lane-evidence.js` now use `writeStableGeneratedJsonReport()` before markdown rendering so identical reruns preserve timestamps; lane evidence timing artifacts and markdown output use `writeTextIfChanged()`. Focused validation and generator refresh are logged in `temp/validation/runner-governance-generated-report-helper-20260521.log`.
- Completed for coverage report artifacts: `tools/testing/coverage/index.js` now writes coverage artifacts through `writeJsonFileResolved()`, and `tools/testing/coverage/policy.js` writes policy JSON through `writeJsonFileResolved()` plus unchanged markdown through `writeTextIfChanged()`. Focused validation is logged in `temp/validation/coverage-report-json-helper-20260521.log`.
- Completed for config/release generated output: `tools/config/inventory.js` now writes timestamp-preserving JSON through `writeStableGeneratedJsonReport()` and markdown through `writeTextIfChanged()`, `tools/config/contract-doc.js` and `tools/release/export-surface-guide.js` write generated markdown through `writeTextIfChanged()`, and `tools/config/generate-demo-config.js` writes generated JSONC through `writeTextIfChanged()` while covering nested `--out` paths. Focused validation is logged in `temp/validation/config-release-writer-helper-20260521.log` and `temp/validation/config-demo-writer-helper-20260521.log`.
- Completed for triage artifacts: `tools/triage/ingest.js` and `tools/triage/decision.js` now share `tools/triage/record-writer.js` for record JSON/markdown output, while `tools/triage/context-pack.js` uses `writeJsonFileResolved()` for context-pack JSON. Focused validation is logged in `temp/validation/triage-writer-helper-focused-clean-20260521.log`; the full context-pack runtime test exceeded the 30-second rule in `temp/validation/triage-writer-helper-20260521.log`.
- Completed for CI diagnostics: `tools/ci/capability-gate.js` now writes capability reports through `writeJsonFileResolved()`, and `tools/ci/run-lsp-embeddings-gates.js` writes diagnostics JSON through `writeJsonFileResolved()` plus JUnit XML through `writeTextIfChanged()`. Focused validation is logged in `temp/validation/ci-writer-helper-20260521.log`.
- Completed for benchmark/report artifacts: `tools/bench/bench-runner.js`, `tools/bench/ab-sweep.js`, `tools/bench/language-summarize.js`, `tools/bench/language-canaries.js`, `tools/bench/language-blocker-closure.js`, and `tools/bench/query-generator.js` now write JSON/text outputs through `writeJsonFileResolved()` and/or `writeTextIfChanged()` where contracts match. Focused validation is logged in `temp/validation/bench-writer-helper-20260521.log`.
- Completed for release generated artifacts: `tools/release/metadata.js`, `tools/release/readiness-gate.js`, `tools/release/check.js`, `tools/release/generate-trust-materials.js`, and `tools/release/assemble-bundle.js` now use shared JSON/text writers for release metadata, readiness, report, manifest, trust, bundle, notes, and checksum artifacts where contracts match. Focused validation is logged in `temp/validation/release-writer-helper-20260521.log` and `temp/validation/release-trust-bundle-writer-helper-20260521.log`.
- Completed for ingest summary artifacts: `tools/ingest/shared.js` now owns `writeIngestSummaryReport()` and `emitIngestSummaryJson()`, and the ctags, gtags, LSIF, and SCIP ingest entrypoints use those helpers for `.meta.json` writes and `--json` summary emission. Focused syntax, ESLint, and ingest contract validation is logged in `temp/validation/ingest-summary-writer-helper-rerun-20260521.log`.
- Completed for `metrics-dashboard`: `tools/reports/metrics-dashboard.js` now emits `--json` stdout through `emitJson()` while preserving its leading blank line and stderr summary contract, and the dashboard test asserts stdout and `--out` JSON stay identical. Focused validation is logged in `temp/validation/cli-search-metrics-strict-harness-20260521.log`.

### 3. Parity harnesses and API fixture setup

Shared modules to prefer:
- [analysis-surface-parity.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\analysis-surface-parity.js)
- [api-server.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\api-server.js)
- [run-node.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers\run-node.js)

2026-05-21 follow-through status: three CLI general route tests now use `tests/helpers/run-node.js` instead of local Node `spawnSync()` wrappers while preserving route-specific assertions. Focused validation is logged in `temp/validation/cli-general-run-node-helper-20260521.log`. A follow-up pass moved the remaining local Node subprocess wrappers in `tests/cli/general` to the same helper, preserving pipe/inherit stdio, custom env, and intentional non-zero assertions. Evidence is logged in `temp/validation/cli-general-run-node-followup-rerun-20260521.log`, with the first no-match scan wrapper failure preserved in `temp/validation/cli-general-run-node-followup-20260521.log`. API stream tests now share `parseSseEvent()`/`parseSseEvents()` and `requestSse()` from `tests/helpers/api-server.js`; syntax, ESLint, parser smoke, and the passing in-memory status SSE check are logged in `temp/validation/api-server-stream-helper-20260521.log` and `temp/validation/api-server-stream-helper-fast-final-20260521.log`, with the two slower API server selectors recorded as 30-second timeouts. Analysis surface parity tests now use `tests/helpers/run-node.js` for CLI execution while preserving intentional invalid-request assertions through `allowFailure: true`; focused validation is logged in `temp/validation/analysis-surface-run-node-helper-20260521.log`. CLI/search direct-run tests and `context-pack/strict-evidence-parity` now reuse the same helper-backed paths instead of local child-process wrappers or production CLI imports where parity harness contracts match; focused validation is logged in `temp/validation/cli-search-metrics-strict-harness-20260521.log`. Runner listing/build-index smoke tests and runner harness artifact/log wrapper tests now use the same helper where child Node execution is expected to succeed; focused validation is logged in `temp/validation/runner-ci-build-index-run-node-helper-rerun-20260521.log` and `temp/validation/runner-harness-run-node-helper-20260521.log`. Tooling config generator/validator/dump tests now use `runNode()` with test-env helpers while preserving invalid-config failure assertions; focused validation is logged in `temp/validation/tooling-config-run-node-helper-20260521.log`. Tooling install/setup JSON and outside-root smoke tests now use `runNode()` with fixture-specific env/PATH behavior preserved; focused validation is logged in `temp/validation/tooling-install-run-node-helper-20260521.log`. TUI wrapper behavior now uses `runNode()` for expected wrapper failures, while TUI build smoke wrappers remain local after their broader focused runner attempt exceeded the 30-second rule; evidence is logged in `temp/validation/tui-wrapper-run-node-helper-20260521.log` and `temp/validation/tui-run-node-helper-20260521.log`. Runner coverage harness tests now use `runNode()` for coverage artifact and coverage flag nested runner calls; focused validation is logged in `temp/validation/runner-coverage-run-node-helper-20260521.log`. Tooling CI wrapper tests now use `runNode()` for successful CI script execution while preserving stdout/stderr assertions; focused validation is logged in `temp/validation/tooling-ci-run-node-helper-20260521.log`. Postinstall patch/rebuild tests now use `runNode()` with `allowFailure` only for expected patch-package failures; focused validation is logged in `temp/validation/postinstall-run-node-helper-20260521.log`. Setup/index-detection/uninstall install-family tests now use `runNode()` while preserving setup JSON/text assertions, artifact-readiness matrix checks, inline index-dir resolution, and destructive fixture cleanup checks; focused validation is logged in `temp/validation/tooling-install-setup-run-node-helper-20260521.log`. Remaining bounded install helper tests now use `runNode()` while preserving empty-PATH failure payloads, phpactor timeout/retry assertions, and lua archive layout failure checks; focused validation is logged in `temp/validation/tooling-install-remaining-run-node-helper-20260521.log`. The synchronous verify-extensions step in the async download-extensions test now uses `runNode()` while the download child remains async `spawn()` for the in-process HTTP server; focused validation is logged in `temp/validation/download-extensions-verify-run-node-helper-20260521.log`. Show-throughput report helper tests now use `runNode()` for report CLI execution while keeping payload and output assertions local; focused validation is logged in `temp/validation/show-throughput-run-node-helper-20260521.log`, `temp/validation/show-throughput-additional-run-node-helper-20260521.log`, and `temp/validation/report-additional-run-node-helper-20260521.log`. The summary report JSON error contract and compare summary helper paths now use `runNode()` for intentional invalid-baseline and compare-models executions; compare-memory and compare-sqlite passed, while summary parity sqlite/sqlite-fts checks exceeded the 30-second cutoff after migration and were not rerun. Evidence is logged in `temp/validation/summary-report-error-run-node-helper-20260521.log` and `temp/validation/summary-report-run-node-helper-20260521.log`. Lexicon report/validate and editor toolchain policy tests now use `runNode()` plus test-env helpers, with expected invalid/missing-toolchain failures handled through `allowFailure`; direct validation with a 30-second wrapper is logged in `temp/validation/lexicon-editor-run-node-helper-20260521.log`. Shared triage, SQLite incremental, and tooling LSP SLO helper wrappers now use `runNode()` while preserving fixture setup, intentional nonzero status assertions, and pipe/inherit stdio behavior; focused validation is logged in `temp/validation/p2-shared-helper-run-node-cluster-20260521-rerun.log`, with the initial expected no-match scan exit preserved in `temp/validation/p2-shared-helper-run-node-cluster-20260521.log`. CI gate smoke tests for tooling LSP default-enable and bench-language rollout now use `runNode()` with `allowFailure` for enforced error cases; focused validation is logged in `temp/validation/ci-gate-smoke-run-node-helper-20260521.log`. The workspace manifest contract matrix now uses `runNode()` for its catalog JSON subprocess; focused validation is logged in `temp/validation/workspace-manifest-run-node-helper-20260521.log`. The reconcile-identity CLI drift test now uses `runNode()` with `allowFailure` for its expected failing report; focused validation is logged in `temp/validation/reconcile-identity-cli-run-node-helper-20260521.log`. Indexing lifecycle build-entry success/failure tests now use `runNode()` while preserving timeout and no-unsettled-warning assertions; focused validation is logged in `temp/validation/indexing-lifecycle-run-node-helper-20260521-rerun.log`. Release blocker-flag rejection and index-diff mode/compact validation tests now use `runNode()` with expected failures preserved through `allowFailure`; focused validation is logged in `temp/validation/release-index-diff-run-node-helper-20260521.log`. The index-stats contract matrix now uses a local `runStats()` wrapper backed by `runNode()`; focused validation is logged in `temp/validation/index-stats-contract-run-node-helper-20260521.log`. Analysis wrapper exit propagation, index config dump, and cache GC tests now use `runNode()` with test-env helpers; focused validation is logged in `temp/validation/analysis-indexdump-cachegc-run-node-helper-20260521.log`. Search-showcase list probes and the Sublime python-policy check now use `runNode()` while leaving Python behavior helpers on Python; focused validation is logged in `temp/validation/showcase-sublime-run-node-helper-20260521-rerun.log`, and the reverted ci-long attempt is preserved in `temp/validation/runner-showcase-sublime-run-node-helper-20260521.log`. Strict JSONL triage ingest expected-failure coverage now uses `runNode()` with `allowFailure`; focused validation is logged in `temp/validation/triage-jsonl-strict-run-node-helper-20260521.log`. Shared-module migration tooling, merge benchmark smoke, embedding-batcher keepalive, and Perl tree-sitter native reset regression tests now use `runNode()`; focused validation is logged in `temp/validation/run-node-small-wrapper-batch-20260521.log`. Search/retrieval/Rust synchronous child wrappers now use `runNode()` while preserving cwd/env, captured stdout, and inherited build output behavior; syntax, ESLint, direct-wrapper scans, compact-json pass, search-startup-fastpath lane pass, and 30-second timeout records are logged in `temp/validation/run-node-search-retrieval-wrapper-batch-20260521.log` and `temp/validation/run-node-search-retrieval-wrapper-batch-lane-followup-rerun-20260521.log`. Query-cache contract wrappers and the backend contract matrix now use `runNode()` while preserving query-cache stdout parsing and backend expected-failure assertions; syntax, ESLint, direct-wrapper scans, the backend pass, and query-cache timeout records are logged in `temp/validation/run-node-query-cache-backend-wrapper-batch-20260521.log` and `temp/validation/run-node-query-cache-wrapper-batch-20260521.log`. HNSW atomic, HNSW ANN, and LanceDB ANN tests now use `runNode()` while preserving optional native dependency gates, inherited build output, and captured search JSON assertions; focused validation is logged in `temp/validation/run-node-ann-wrapper-batch-20260521.log`. Shared build/search fixture helpers now use `runNode()` for Node subprocess execution while preserving crash-log tailing, custom throw/exit behavior, fixture cwd/env, inherited build output, search JSON parsing, and formatted command-failure output; representative caller validation is logged in `temp/validation/run-node-helper-wrapper-batch-20260521.log`. Indexing validate/import/filter/format wrapper tests now use `runNode()` while preserving expected-failure validation assertions, captured stderr/stdout checks, inherited build output, fixture cwd/env, and JSON validation parsing; focused validation is logged in `temp/validation/run-node-indexing-wrapper-batch-20260521.log`. The import-links indexing regression now uses `runNode()` while preserving repo cwd/env, inherited build output, and file-relation import-link assertions; focused validation is logged in `temp/validation/run-node-import-links-wrapper-20260521.log`. The comment-join indexing regression now uses `runNode()` while preserving captured build/search output and comment/extracted-prose/no-comments assertions; focused validation is logged in `temp/validation/run-node-comment-join-wrapper-20260521.log`.

Representative local implementations:
- [risk-explain-surface-parity.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\analysis\risk-explain-surface-parity.test.js)
- [risk-delta-surface-parity.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\analysis\risk-delta-surface-parity.test.js)
- [strict-evidence-parity.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\context-pack\strict-evidence-parity.test.js)
- [risk-filters-parity.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\context-pack\risk-filters-parity.test.js)
- [federated-risk-parity.test.js](C:\Users\sneak\Development\DOUBLECLEAT\tests\context-pack\federated-risk-parity.test.js)

Best action:
- Keep extending these existing helpers instead of letting each parity suite grow bespoke setup.
- Completed for `analysis-surface-parity.js`: CLI execution now shares `runNode()` rather than importing `spawnSync` directly. `context-pack/risk-filters-parity` remains covered by prior timeout evidence rather than a repeated rerun.
- Completed for `tests/cli/general`: local Node CLI invocations now share `runNode()`, and a focused scan confirms no remaining `spawnSync(process.execPath, ...)` in that directory.
- Completed for `tests/context-pack/strict-evidence-parity.test.js`: the CLI assertion now uses `createAnalysisSurfaceHarness().runCli()` instead of importing `runContextPackCli()` directly.
- Completed for `tests/cli/search/non-result-surfaces.test.js`, `tests/dispatch/search-flag-passthrough.test.js`, and `tests/cli/error-contract.test.js`: child Node execution now uses `runNode()` and test env helpers, with explicit no-test-env legacy warning checks left local.
- Completed for runner/build-index smoke tests: `tests/cli/build-index/all.test.js`, `tests/ci/suite-runner.smoke.test.js`, and `tests/runner/list-json-lane-explanation.test.js` now use `runNode()`. The originally deferred `tests/runner/test-runner.js` stale `runner/harness/skip-semantics` assertion was later repaired to the current `runner/harness/skip-target` discovery entry.
- Completed for runner harness artifact/log tests: profile artifact, profile normalization, report-file, stability artifact, and log runId contract tests now use `runNode()` for nested runner calls.
- Completed for tooling config tests: demo-config output and config contract-matrix script invocations now use `runNode()`, including `allowFailure` for the expected invalid-config validator case.
- Completed for tooling install/setup tests: tooling install dry-run, bootstrap JSON output, setup JSON output, and tool-root search smoke now use `runNode()` while preserving fixture-specific environment behavior.
- Completed for `tests/tui/wrapper-behavior.test.js`: wrapper invocations now use `runNode()` with `allowFailure` for expected failure modes. `tui/build-verify-manifest` and `tui/headless-smoke` stay local because their focused runner attempt timed out under the 30-second rule.
- Completed for runner coverage harness tests: coverage equals-form and coverage flag list checks now use `runNode()` for nested runner calls.
- Completed for tooling CI wrapper tests: get-last-failure latest-pointer and command-surface audit now use `runNode()` for CI script execution.
- Completed for postinstall tests: patch enforcement and rebuild-native contract tests now use `runNode()` while preserving expected failure assertions.
- Completed for setup/index/uninstall tests: setup text/JSON smoke, setup index artifact detection, and tooling uninstall cleanup now use `runNode()` for child setup/uninstall execution.
- Completed for remaining bounded install helpers: empty-PATH install failure callers, phpactor network guards, and lua-language-server archive layout tests now use `runNode()` while preserving expected non-zero assertions.
- Completed for the download-extensions verify step: the synchronous verify subprocess now uses `runNode()`; async download subprocesses stay local because they depend on an in-process HTTP server.
- Completed for show-throughput report helper tests: compare-profile, JSON contract, scan-outcomes, and scan-profile-preferred tests now execute the report CLI through `runNode()` via their local fixture helpers.
- Completed for additional report tests: show-throughput profiles/filtering/language/stats/ledger, materialize-throughput deep-analysis, bench-language summarize, and summary JSON error contract now use `runNode()` or the helper-backed show-throughput wrapper.
- Completed for summary compare helper tests: compare-memory and compare-sqlite now execute compare-models through `runNode()`. Summary parity sqlite/sqlite-fts wrappers also migrated, but their exact checks timed out under the 30-second policy and remain behavior-proof follow-up.
- Completed for small lexicon/editor policy tests: lexicon report/validate, VS Code missing-toolchain, and Sublime python-toolchain policy tests now use `runNode()` with helper-built envs and explicit expected-failure handling.
- Completed for shared helper wrappers: `tests/helpers/triage.js`, `tests/helpers/sqlite-incremental.js`, and `tests/helpers/tooling-lsp-slo-gate.js` now use `runNode()` for child Node execution while keeping fixture ownership and expected-failure assertions local.
- Completed for CI gate smoke tests: tooling LSP default-enable and bench-language rollout gate smoke tests now use `runNode()` with `allowFailure` for enforced error cases.
- Completed for `tests/workspace/manifest-contract-matrix.test.js`: the catalog JSON subprocess now uses `runNode()` while workspace fixture and manifest assertions remain local.
- Completed for `tests/tooling/index/reconcile-identity-cli.test.js`: expected failing CLI execution now uses `runNode()` with `allowFailure` while preserving JSON drift assertions.
- Completed for indexing lifecycle build-entry tests: success and expected-failure child process checks now use `runNode()` with existing test env helpers.
- Completed for release/index-diff wrapper tests: unsupported release flags and index-diff compact rejection now use `runNode()` with expected failures preserved.
- Completed for `tests/tooling/index-stats/contract-matrix.test.js`: index-stats subprocess calls now share a local `runStats()` wrapper backed by `runNode()`.
- Completed for analysis/index-dump/cache-GC wrappers: expected CLI errors, config dump, and cache GC tool execution now use `runNode()` with helper-built envs.
- Completed for search-showcase/Sublime policy wrappers: Node list/policy probes now use `runNode()` while Python behavior probes remain Python-owned.
- Completed for strict JSONL triage ingest: expected malformed-input failure now uses `runNode()` with shared triage fixture env.
- Completed for shared-module migration, merge benchmark, embedding-batcher, and Perl reset wrappers: bounded child Node probes now use `runNode()` with expected failure handling where needed.
- Completed for indexer/extracted-prose wrappers: `tests/indexer/metav2/contract-matrix.test.js`, `tests/indexer/call-sites/contract-matrix.test.js`, and `tests/indexing/extracted-prose/core.test.js` now use `runNode()` for build/search child Node execution while preserving fixture cwd/env isolation, custom failure assertions, and captured JSON parsing. Syntax, targeted ESLint, and direct-wrapper scans passed in `temp/validation/run-node-indexer-extracted-prose-wrapper-timeboxed-20260521.log`; `indexer/metav2/contract-matrix` passed in 52.8s in `temp/validation/run-node-indexer-extracted-prose-wrapper-batch-rerun-20260521.log`, while direct `call-sites` and `extracted-prose/core` behavior checks were stopped at the 30-second cutoff in the timeboxed log.
- Completed for small artifact bench/clean wrappers: `tests/indexing/artifacts/file-meta-bench-contract.test.js`, `tests/indexing/artifacts/minhash-packed-bench-contract.test.js`, and `tests/indexing/artifacts/clean.test.js` now use `runNode()` for bounded tool subprocesses while preserving captured bench output assertions and inherited clean-artifacts output. Syntax, targeted ESLint, direct-wrapper scans, and all three direct 30-second checks passed in `temp/validation/run-node-artifact-bench-clean-wrapper-20260521.log`.
- Completed for tooling ingest wrappers: ctags, gtags, lsif, and scip ingest tests plus the missing-input helper now use `runNode()` for CLI subprocesses while preserving captured JSON/error output, repo fixture setup, escape-path filtering assertions, and expected missing-input failures through `allowFailure`. Syntax, targeted ESLint, direct-wrapper scans, and all four direct 30-second checks passed in `temp/validation/run-node-tooling-ingest-wrapper-20260521.log`.
- Completed for tooling eval wrappers with caveat: `tests/tooling/eval/risk-pack-quality.test.js` and `tests/tooling/eval/quality.test.js` now use `runNode()` for child Node execution while preserving captured JSON assertions and the inherited build output path. Syntax, targeted ESLint, and direct-wrapper scans passed in `temp/validation/run-node-tooling-eval-wrapper-20260521.log`; `risk-pack-quality` passed in that log, while `eval-quality` was stopped at the 30-second cutoff during `build_index` and needs a future proof strategy before claiming behavior coverage.
- Completed for tooling triage/cache/structural wrappers: decision finding path-safety, cache GC contract matrix, and structural search tests now use `runNode()` while preserving expected nonzero triage assertions, cache JSON parsing, fixture PATH setup, and structural result assertions. Syntax, targeted ESLint, direct-wrapper scans, and all three direct 30-second checks passed in `temp/validation/run-node-tooling-triage-cache-structural-wrapper-20260521.log`.
- Completed for perf bench tooling wrappers: the bench-runner fixture, guardrails contract, output-schema, per-output-schema, and AB sweep contract tests now use `runNode()` for child Node execution while preserving captured JSON payload parsing, expected guardrail failure assertions, and fixture env setup. Syntax, targeted ESLint, direct-wrapper scans, four direct contract checks, and both fixture consumers passed in `temp/validation/run-node-perf-bench-wrapper-20260521.log` and `temp/validation/run-node-perf-bench-wrapper-consumers-20260521.log`.
- Completed for CI LSP gate wrappers: LSP embeddings gate testing-env, timeout JUnit, and tooling-LSP guardrail regression-diff tests now use `runNode()` while preserving explicit gate timeouts, expected timeout exit assertions, diagnostics/JUnit parsing, and guardrail diff payload assertions. Syntax, targeted ESLint, direct-wrapper scans, and all three direct 30-second checks passed in `temp/validation/run-node-ci-lsp-gate-wrapper-20260521.log`.
- Completed for small benchmark contract wrappers: artifact-IO throughput, SQLite build-from-artifacts, and chargram-postings bench contract tests now use `runNode()` while preserving captured bench output assertions and SQLite fixture env setup. Syntax, targeted ESLint, direct-wrapper scans, and all three direct 30-second checks passed in `temp/validation/run-node-small-bench-contract-wrapper-20260521.log`.
- Completed for Sublime packaging wrappers: package structure, release sanity, determinism, and archive metadata tests now use `runNode()` for `tools/package-sublime.js` child Node calls while preserving package output, manifest, checksum, command/menu/keymap/settings, and metadata assertions. Syntax, targeted ESLint, direct-wrapper scans, and all four direct 30-second checks passed in `temp/validation/run-node-sublime-package-wrapper-20260521.log`.
- Completed for VS Code packaging wrappers: determinism, archive metadata, and contract matrix tests now use `runNode()` for `tools/package-vscode.js` child Node calls while preserving package output, manifest, walkthrough, configuration, command/menu/keybinding, and shipped-entry assertions. Syntax, targeted ESLint, direct-wrapper scans, and all three direct 30-second checks passed in `temp/validation/run-node-vscode-package-wrapper-20260521.log`.
- Completed for runtime/lifecycle wrappers: shared lifecycle and runtime contract matrices now use `runNode()` for synchronous child Node probes while preserving the lifecycle unref keepalive timeout, wrapper config-dump JSON assertions, and the intentionally local async lifecycle keepalive child. Syntax, targeted ESLint, synchronous direct-wrapper scans, and both direct 30-second checks passed in `temp/validation/run-node-runtime-lifecycle-wrapper-20260521.log`.
- Completed for install/fixture wrappers with caveat: tooling install detect/plan and fixture-eval now use `runNode()` for child Node execution while preserving JSON parsing, fixture env, inherited build output, and expected success handling. Syntax, targeted ESLint, direct-wrapper scans, and the install detect/plan direct check passed in `temp/validation/run-node-install-fixture-wrapper-20260521.log`; fixture-eval hit the 30-second cutoff during a real index build and needs a future behavior-proof strategy before claiming direct runtime coverage.
- Completed for synthetic index/build wrappers with caveats: manifest embeddings pieces, embeddings validation, incremental manifest, two-stage state, structural filters, and the map build fixture now use `runNode()` for child Node execution while preserving cache/test env setup, stub embeddings, inherited build output, and artifact assertions. Syntax, targeted ESLint, and direct-wrapper scans passed in `temp/validation/run-node-synthetic-index-build-wrapper-20260521.log`; `indexing/embeddings/manifest-pieces` and `tooling/structural/filters` passed under 30 seconds, `indexing/incremental/manifest` printed its pass message before `timed_out_after_pass`, and `indexing/embeddings/validate`, `indexing/runtime/two-stage-state`, and `map/build-contract-matrix` hit the 30-second cutoff.
- Completed for Sublime Python-policy wrappers with caveat: pycompile and package-harness now use `runNode()` for the Node `python-check.js` policy probe while actual Python subprocesses stay local to the Python behavior checks. Syntax, targeted ESLint, direct Node-wrapper scans, retained-Python-call scans, and `tooling/sublime/pycompile` passed in `temp/validation/run-node-sublime-python-policy-wrapper-20260521.log`; `tooling/sublime/package-harness` hit the 30-second cutoff and needs a future behavior-proof strategy before claiming direct runtime coverage.
- Completed for runner list wrappers: `tests/runner/test-runner.js` and `tests/runner/ci-long-order-selection.test.js` now use `runNode()` for runner list/error subprocesses. The stale `skip-semantics`, hardcoded ci-long entry, soak-entry, and `>50` assertions were repaired against the current ordered-manifest contract while preserving list JSON, selection-source, selection-lane, non-empty, non-long, and expected-failure assertions. Syntax, targeted ESLint, direct-wrapper scans, direct `tests/runner/test-runner.js`, and `runner/ci-long-order-selection` passed in `temp/validation/run-node-runner-list-wrapper-20260521.log`; current-shape proof is logged in `temp/validation/runner-list-current-proof-20260521.log` and `temp/validation/runner-ci-long-current-shape-20260521.log`.
- Completed for artifact build wrappers with caveat: packed artifact fastpath, dynamic write-concurrency ordering, and artifact-size guardrail tests now use `runNode()` for build subprocesses while preserving inherited build output, cache/test env setup, and artifact assertions. Syntax, targeted ESLint, and direct-wrapper scans passed in `temp/validation/run-node-artifact-build-wrapper-20260521.log`; `indexing/artifacts/packed-artifact-fastpath` and `indexing/artifacts/dynamic-write-concurrency-preserves-order` passed under 30 seconds, while `indexing/artifacts/artifact-size-guardrails` hit the 30-second cutoff during build output.
- Completed for code-map wrappers with caveat: guardrail and contract matrix tests now use `runNode()` for build and report-code-map child Node calls while preserving fixture env setup, inherited build output, captured JSON/dot output, and expected status assertions. Syntax, targeted ESLint, and direct-wrapper scans passed in `temp/validation/run-node-code-map-wrapper-20260521.log`; both code-map selectors hit the 30-second cutoff during fixture build setup and need future behavior-proof strategy before claiming direct runtime coverage.
- Completed for smoke wrappers with caveat: smoke utility, e2e, and retrieval wrappers now use `runNode()` for child Node calls while preserving smoke helper APIs, fixture env setup, inherited build output, captured search/map output, and the non-Node Graphviz `dot` probe. Syntax, targeted ESLint, direct Node-wrapper scans, and retained-Graphviz scans passed in `temp/validation/run-node-smoke-wrapper-20260521.log`; `smoke/e2e` and `smoke/retrieval` hit the 30-second cutoff during build/index work.
- Completed for policy/tooling wrappers: optional dependency policy, shared-module performance, Python contract fail-open, and tooling navigation-query tests now use `runNode()` for child Node calls while preserving expected skip/failure statuses, shared test env setup, performance JSON assertions, Python external-command probing, and navigation CLI output assertions. Syntax, targeted ESLint, direct Node-wrapper scans, retained-Python-probe scans, and all four direct 30-second checks passed in `temp/validation/run-node-policy-tooling-wrapper-20260521.log`.
- Completed for piece-assembly wrappers with caveat: core and missing-manifest tests now use `runNode()` for build/assemble child Node calls while preserving inherited build output, expected manifest failure handling, cache/test env setup, and assembled artifact assertions. Syntax, targeted ESLint, and direct-wrapper scans passed in `temp/validation/run-node-piece-assembly-wrapper-20260521.log`; `indexing/piece-assembly/assemble-pieces-no-guess` passed under 30 seconds, while `indexing/piece-assembly/core` hit the 30-second cutoff after a successful build sub-step.
- Completed for additional test child-Node wrapper adoption: CI import-resolution SLO gate, risk-explain adapter CLI, encoding fallback, indexer-service CLI, TUI installer/build/capture wrappers, SQLite storage wrappers, LMDB storage wrappers, service SQLite/smoke wrappers, indexing metadata/metrics/records/file-processor/chunk-id wrappers, and retrieval filter wrappers now use `runNode()` where the subprocess contract is ordinary child Node execution. The migration preserved captured JSON/stdout/stderr, inherited build output, expected nonzero assertions via `allowFailure`, test-env setup, cwd, and timeout behavior. Evidence is logged in `temp/validation/run-node-ci-risk-encoding-wrapper-20260521.log`, `temp/validation/run-node-encoding-fallback-wrapper-rerun-3-20260521.log`, `temp/validation/run-node-indexer-service-wrapper-20260521.log`, `temp/validation/run-node-tui-wrapper-20260521.log`, `temp/validation/run-node-sqlite-storage-wrapper-20260521.log`, `temp/validation/run-node-sqlite-storage-rerun-20260521.log`, `temp/validation/run-node-lmdb-storage-wrapper-20260521.log`, `temp/validation/run-node-service-sqlite-wrapper-20260521.log`, `temp/validation/run-node-indexing-artifact-wrapper-20260521.log`, and `temp/validation/run-node-retrieval-filter-wrapper-20260521.log`.
- Completed for indexing determinism wrappers: chunk UID determinism, symbol artifact determinism, incremental cache signature, incremental tokenization cache, and chunk-meta determinism now use `runNode()` for build-index child Node execution while preserving inherited build output, captured failure formatting, cache/test env setup, and direct status assertions. Syntax, targeted ESLint, touched-file direct-wrapper scans, and all five focused 30-second runner checks passed in `temp/validation/run-node-indexing-determinism-wrapper-20260521.log`.
- Completed for the remaining ordinary indexing wrappers: embeddings explicit-index-root missing-mode, extracted-prose chunk-id collision, file-caps, Git blame range, vector-only postings policy/pending-artifacts, SCM provider/build-id, shard merge/progress determinism, and type-inference crossfile/provider child Node calls now use `runNode()` while preserving expected failures, captured output, inherited build output, timeout options, and fixture env setup. The pass also fixed SCM annotate so file-level metadata unavailable results do not suppress line-level blame authors. Evidence is logged in `temp/validation/run-node-indexing-safe-wrapper-final-20260521.log`, `temp/validation/git-blame-annotate-unavailable-fix-20260521.log`, and the initial batch log `temp/validation/run-node-indexing-safe-wrapper-batch-20260521.log`; `indexing/file-caps/contract-matrix` hit the 30-second cutoff and remains a behavior-proof follow-up.
- Completed for the scan-20 ordinary non-indexing wrappers: script-coverage group runner, shared logging contract matrix, retrieval parity equivalence build stages, and SQLite incremental scenario search helpers now use `runNode()` while preserving inherited output, captured stdout/stderr assertions, explicit status checks, and parsed search JSON. Current docs/reference validation for this slice passed in `temp/validation/run-node-non-indexing-safe-docs-20260521.log`; focused runner proof passed `shared/logging/contract-matrix` and `tooling/script-coverage/core`, while `retrieval/parity/equivalence` and `storage/sqlite/incremental/update-contract-matrix` hit the 30-second cutoff in `temp/validation/run-node-non-indexing-safe-focused-tests-20260521.log`.
- Completed for the scan-21 ordinary perf, optional-backend, graph, daemon, and indexing micro-benchmark wrappers: these now use `runNode()` while preserving captured JSON/stdout/stderr, inherited build output, explicit expected-nonzero assertions, timeout skip handling, optional dependency gates, Node flag self-reexec, and search/build status checks. The pass also repaired the perf-budget regression test to target the current `runner/harness/pass-target` selector. Current docs/reference validation for this slice passed in `temp/validation/run-node-safe-batch2-docs-20260521.log`; the focused batch passed 15 selectors in `temp/validation/run-node-safe-batch2-focused-tests-20260521.log`, the repaired selector passed in `temp/validation/run-node-safe-batch2-regression-gate-rerun-20260521.log`, and seven build-heavy selectors timed out under the 30-second policy.
- Completed residual no-adopt enforcement: `tests/tooling/run-node-residual-policy.test.js` now scans `tests/**` for direct `spawnSync(process.execPath, ...)` calls and fails if the residual file set or per-file counts drift outside the explicit process-policy owners. Syntax, targeted ESLint, and the focused policy selector passed in `temp/validation/run-node-residual-policy-20260521.log`.
- Current residual direct Node wrapper scan: `temp/validation/run-node-current-direct-wrapper-scan-23-20260521.log` records 21 match lines across 13 files. The remaining files are intentionally enforced process-policy classes: `tests/helpers/run-node.js`, benchmark runner orchestration, runner meta/harness process-behavior tests, and shared subprocess timeout/abort/quoting/leak tests. No ordinary scan-20 or scan-21 candidate remains.

## Healthy No-Adopt Zones

- [tests/helpers](C:\Users\sneak\Development\DOUBLECLEAT\tests\helpers)
  - healthy local shared test surface
- [benchmarks/repos](C:\Users\sneak\Development\DOUBLECLEAT\benchmarks\repos)
  - fixture payload, not adoption target
- [tools/bench](C:\Users\sneak\Development\DOUBLECLEAT\tools\bench)
  - share low-level helpers selectively, keep workload semantics local
- [show-throughput.js](C:\Users\sneak\Development\DOUBLECLEAT\tools\reports\show-throughput.js)
  - domain-specific aggregation/rendering should remain local

## Recommended Follow-On Issues

- `#426`: if low-level JSON/report/file boilerplate still feels too fragmented after H31
- `#428`: for the shared test-harness/helper follow-on work
- `#432`: for prioritizing the concrete cleanup queue after the full shared-module scan program is done
