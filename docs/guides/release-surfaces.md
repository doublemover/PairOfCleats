# Release Surfaces

This file is generated by `node tools/release/export-surface-guide.js`.

Registry: `docs/tooling/shipped-surfaces.json`

The release pipeline, artifact verification, and smoke contracts should consume the same surface registry described below.

## Canonical CLI

- id: `cli`
- owner: `cli`
- support: `stable`
- packaging boundary: `node-bin`
- publish boundary: `repo-package`
- version source: `package.json#version`
- runtime targets: `node>=24.13.0`
- platforms: `windows`, `linux`, `macos`

Build contract: `source-entrypoint`

- build source: `bin/pairofcleats.js`

Install contract: `npm-bin-or-direct-node`
- Expose `pairofcleats` on PATH via install/link, or run `node bin/pairofcleats.js` directly from source.

Smoke contract:
- CLI boots, reports its version, builds a fixture index, validates it, and returns a search result.
- automated release-check steps:
  - `smoke.version` -- pairofcleats --version
  - `smoke.fixture-index-build` -- fixture index build
  - `smoke.fixture-index-validate-strict` -- fixture index validate --strict
  - `smoke.fixture-search` -- fixture search

## HTTP API server

- id: `api`
- owner: `services-api`
- support: `stable`
- packaging boundary: `node-service`
- publish boundary: `repo-package`
- version source: `package.json#version`
- runtime targets: `node>=24.13.0`
- platforms: `windows`, `linux`, `macos`

Build contract: `source-service`

- build source: `tools/api/server.js`
- build source: `tools/api/router.js`

Install contract: `node-service-launch`
- Launch via `pairofcleats service api` or `node tools/api/server.js` with repo-local config and token policy.

Smoke contract:
- Boot the packaged/runtime entrypoint, probe health/status/capabilities, and validate one representative API workflow.
- automated release-check steps: _not yet enabled_

## MCP server

- id: `mcp`
- owner: `services-mcp`
- support: `operator`
- packaging boundary: `node-service`
- publish boundary: `repo-package`
- version source: `package.json#version`
- runtime targets: `node>=24.13.0`
- platforms: `windows`, `linux`, `macos`

Build contract: `source-service`

- build source: `tools/mcp/server.js`

Install contract: `node-service-launch`
- Launch via `pairofcleats service mcp` or the server entrypoint and attach over stdio/jsonrpc.

Smoke contract:
- Boot the MCP server, complete initialize/capabilities handshake, and execute one representative tool call.
- automated release-check steps: _not yet enabled_

## Indexer service mode

- id: `indexer-service`
- owner: `services-indexer`
- support: `operator`
- packaging boundary: `node-service`
- publish boundary: `repo-package`
- version source: `package.json#version`
- runtime targets: `node>=24.13.0`
- platforms: `windows`, `linux`, `macos`

Build contract: `source-service`

- build source: `tools/service/indexer-service.js`

Install contract: `node-service-launch`
- Launch via `pairofcleats service indexer` or `node tools/service/indexer-service.js`.

Smoke contract:
- Boot in smoke mode and verify the service command path responds with a valid JSON health/contract payload.
- automated release-check steps:
  - `smoke.service-mode` -- service-mode smoke check

## VS Code extension

- id: `vscode`
- owner: `editor-vscode`
- support: `stable`
- packaging boundary: `vsix`
- publish boundary: `vsix-artifact`
- version source: `extensions/vscode/package.json#version`
- runtime targets: `node>=24.13.0`, `vscode-extension-host`
- platforms: `windows`, `linux`, `macos`

Build contract: `package-script`

- build source: `extensions/vscode/package.json`
- build source: `tools/package-vscode.js`
- build output: `dist/vscode/pairofcleats.vsix`
- build output: `dist/vscode/pairofcleats.vsix.sha256`

Install contract: `vsix-install`
- Install the VSIX into a compatible VS Code host and validate activation/runtime packaging metadata.

Smoke contract:
- Package the VSIX deterministically and verify the smoke contract for manifest, activation, and bundled assets.
- automated release-check steps:
  - `smoke.editor-vscode` -- editor package smoke (vscode)

## Sublime package

- id: `sublime`
- owner: `editor-sublime`
- support: `operator`
- packaging boundary: `sublime-package`
- publish boundary: `archive-artifact`
- version source: `package.json#version`
- runtime targets: `python>=3.8`, `sublime-text-host`
- platforms: `windows`, `linux`, `macos`

Build contract: `package-script`

- build source: `sublime/PairOfCleats`
- build source: `tools/package-sublime.js`
- build output: `dist/sublime/pairofcleats.sublime-package`
- build output: `dist/sublime/pairofcleats.sublime-package.sha256`

Install contract: `sublime-package-install`
- Install the generated `.sublime-package` into the target Sublime environment and validate package metadata.

Smoke contract:
- Package the Sublime surface deterministically and verify the smoke contract for bundled files and archive integrity.
- automated release-check steps:
  - `smoke.editor-sublime` -- editor package smoke (sublime)

## Rust TUI and wrapper

- id: `tui`
- owner: `tui`
- support: `operator`
- packaging boundary: `native-binary-plus-wrapper`
- publish boundary: `platform-artifact`
- version source: `package.json#version`
- runtime targets: `rust-target-artifact`, `node>=24.13.0`
- platforms: `windows`, `linux`, `macos`

Build contract: `cargo-plus-staging`

- build source: `crates/pairofcleats-tui/Cargo.toml`
- build source: `bin/pairofcleats-tui.js`
- build source: `tools/tui/build.js`
- build source: `tools/tui/install.js`
- build output: `dist/tui/tui-artifacts-manifest.json`
- build output: `dist/tui/tui-artifacts-manifest.json.sha256`
- build output: `dist/tui/install-smoke`

Install contract: `wrapper-verified-install`
- Stage host-target binaries, install them into the shared cache-root layout, and launch through `pairofcleats-tui`.

Smoke contract:
- Build and checksum the staged TUI artifacts, then perform an install smoke against an isolated install root.
- automated release-check steps:
  - `smoke.tui-build` -- tui artifact manifest smoke
  - `smoke.tui-install` -- tui install smoke
